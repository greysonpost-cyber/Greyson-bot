const {SlashCommandBuilder,ActionRowBuilder,ButtonBuilder,ButtonStyle,EmbedBuilder,ChannelType}=require('discord.js');const db=require('../../database/db');const arts=require('../../services/artifacts');const eco=require('../../services/economy');
module.exports={data:new SlashCommandBuilder().setName('market').setDescription('Artifact marketplace')
.addSubcommand(s=>s.setName('list').setDescription('List an artifact for Power Tokens').addIntegerOption(o=>o.setName('artifact_id').setRequired(true).setDescription('Owned artifact ID')).addIntegerOption(o=>o.setName('price').setRequired(true).setMinValue(1).setDescription('Price')).addChannelOption(o=>o.setName('channel').addChannelTypes(ChannelType.GuildText).setDescription('Marketplace channel')))
.addSubcommand(s=>s.setName('browse').setDescription('Browse active listings'))
.addSubcommand(s=>s.setName('cancel').setDescription('Cancel your listing').addIntegerOption(o=>o.setName('listing_id').setRequired(true).setDescription('Listing ID'))),
async execute(i){const s=i.options.getSubcommand(),g=i.guild.id;if(s==='list'){const id=i.options.getInteger('artifact_id'),a=arts.artifact(id);if(!a||a.owner_id!==i.user.id)return i.reply({content:'❌ You do not own that artifact.',ephemeral:true});if(a.locked_trade_id)return i.reply({content:'❌ That artifact is locked in a trade.',ephemeral:true});if(db.prepare(`SELECT 1 FROM marketplace_listings WHERE artifact_id=? AND status='active'`).get(id))return i.reply({content:'❌ Already listed.',ephemeral:true});const c=i.options.getChannel('channel')||i.channel,p=i.options.getInteger('price');const r=db.prepare(`INSERT INTO marketplace_listings(guild_id,artifact_id,seller_id,price,channel_id,created_at) VALUES(?,?,?,?,?,?)`).run(g,id,i.user.id,p,c.id,Date.now());const lid=r.lastInsertRowid,e=new EmbedBuilder().setColor(0x00C8FF).setTitle(`💰 ${arts.label(a)}`).setDescription(`Seller: ${i.user}\nPrice: **${p} Power Tokens**\nCollection: **${a.collection_name}**\nListing ID: **${lid}**`).setFooter({text:'Instant purchase transfers ownership safely'});const m=await c.send({embeds:[e],components:[new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId(`econ_marketbuy:${lid}`).setLabel(`Buy for ${p} PT`).setStyle(ButtonStyle.Success))]});db.prepare('UPDATE marketplace_listings SET message_id=? WHERE id=?').run(m.id,lid);return i.reply({content:`✅ Listed in ${c}.`,ephemeral:true})}if(s==='browse'){const rows=db.prepare(`SELECT l.*,a.copy_number,t.name,t.collection_name FROM marketplace_listings l JOIN artifacts a ON a.id=l.artifact_id JOIN artifact_types t ON t.id=a.type_id WHERE l.guild_id=? AND l.status='active' ORDER BY l.created_at DESC LIMIT 20`).all(g);return i.reply({content:`💰 **Active Marketplace**\n${rows.map(x=>`#${x.id} • **${x.name} #${String(x.copy_number).padStart(3,'0')}** — ${x.price} PT — <@${x.seller_id}>`).join('\n')||'No active listings.'}`})}const l=db.prepare(`SELECT * FROM marketplace_listings WHERE id=? AND guild_id=? AND status='active'`).get(i.options.getInteger('listing_id'),g);if(!l||l.seller_id!==i.user.id&&!eco.isOwner(i))return i.reply({content:'❌ Listing not found or not yours.',ephemeral:true});db.prepare(`UPDATE marketplace_listings SET status='cancelled' WHERE id=?`).run(l.id);if(l.channel_id&&l.message_id){const c=i.guild.channels.cache.get(l.channel_id),m=await c?.messages.fetch(l.message_id).catch(()=>null);await m?.edit({components:[]}).catch(()=>{})}return i.reply({content:'✅ Listing cancelled.',ephemeral:true})}};
