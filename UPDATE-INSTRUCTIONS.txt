DripCore v2.2.7 SMALL UPDATE

1. Open this ZIP.
2. Upload the included src folder into the ROOT of your GitHub repository.
3. Replace the 5 matching files only.
4. Do NOT upload or replace your data folder or bot.sqlite database.
5. Let Railway redeploy.
6. Redeploy slash commands so the new configuration commands appear.

After deployment, configure escalation roles:
/giveaway-config staff-role
/giveaway-config manager-role

Configure /giverole access more easily:
/role-permissions manage granter_role:<role>
Then select every role that role may give/remove.

This patch expects the previous v2.2.6 giveaway update to already be installed.
